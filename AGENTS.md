# 数学建模项目规则

本文件只约束其所在数学建模项目根目录及其子目录中的任务。

## 公共规则的唯一来源

- 本文件集中定义项目的模式、默认配置、授权与人工判断、证据纪律、Git、结果交接和公共汇报规则。项目级 Skills 与 references 引用本文件，不另行维护不同版本的公共政策。
- 用户的指令优先于 Skill 中提供的指南。如果用户的明确指令与 Skill 的指令冲突，应优先遵循用户的指令。此优先级不越过系统指令、开发者指令或工具权限；赛题配置可以覆盖比赛、语言和求解默认值，但 AI 建议、猜想、疑问、倾向性表达或探索性赞同不能记作人工决定。
- 如果某个 Skill 导致你请求许可或确认、暂停、使请求的工作未完成，或偏离用户的意图，应指出并链接实际读取的确切 `SKILL.md` 文件，引用相关指令，并简要说明该指令如何适用。区分 Skill 的明确要求与对指南的解释。
- Skill 保留本领域的输入输出、算法说明、操作步骤和检查项；其中的前置条件用于核验证据，不代表增加一次人工确认。程序中的字段、阶段门和校验属于可执行契约，应与本文件一致。
- 明确的 Harness 维护任务不初始化赛题状态，也不套用赛题方法选择或结果判断门。

## 默认入口

- 完整赛题、目标模糊的请求、跨阶段任务，以及需要继续既有建模工作的请求，优先使用 `math-modeling-manager`。
- 用户明确说“先讨论思路”“评价方案”“暂不写代码”时，使用 `modeling-thought-partner`。讨论模式不更新阶段门、不生成正式产物，也不把建议当作方法决定；用户明确要求正式求解、采用方法或开始实现时再回到总入口。
- 自由讨论可在任意建模阶段进行：允许主动抽象、推导、提出新模型、比较或推翻方案，不受固定 Skill 顺序、既定算法清单或阶段门限制。阶段门约束正式执行和成果接受；讨论中的假设与猜想应明确标识，已有证据与事实仍需如实表达。
- 用户明确要求单项工作时，可以直接使用对应专业 Skill；完成后把产物交回 `workflow-orchestrator` 更新状态。
- `math-modeling-manager` 负责判断阶段和路由，`workflow-orchestrator` 负责 manifest、阶段门和下一动作，专业 Skill 负责实际工作。不要让总入口重复执行专业分析。

## 默认配置

- 默认比赛为 CUMCM，工作语言为简体中文。
- 完整赛题默认 `rigor_profile=submission`；局部实验、学习、临时分析默认 `lean`。
- 使用Python作为主要的编程语言

## 自动推进与人工判断

在权限范围内自动推进文件盘点、数据剖析、可复现计算、代码执行、检查、结果展示和状态更新。仅在下列判断会实质改变模型或结果结论时暂停确认：

1. 题意或成功标准存在重大歧义；
2. 主方法与参考策略的选择；
3. 结果接受、调整或启用备选方案；

人工决定由 `workflow-orchestrator record-decision` 记入每问的 JSONL 决策账本。不得把 AI 建议当成人工决定。选择卡只是当前专业 Skill 或调度器的对话输出，不再作为独立流程步骤。

- 已明确选定的方法直接记录，不重复询问同一选择；用户未给出理由时记录为 null，不要求补写理由。探索性赞同仍不等于正式决定。
- 模型假设的必要性、物理解释、推导和贡献分析可由 AI 基于证据提出；最终采用受上述方法与结果判断约束，不逐条新增确认。
- 学术证据扫描是正式完整求解的准备步骤，不阻止用户在任意阶段主动发起自由讨论。
- 缺失实际数据、工具不可用或缺少访问权限属于执行阻塞，不是新增的建模判断类别；如实说明，不伪造通过。因局部问题受阻时可继续独立且已授权的工作。
- 已完成步骤是否有效，以当前输入、输出、依赖和决定的哈希绑定为准；不能仅凭文件存在或时间戳较新认定有效。证据实质改变后重新验证，涉及既有人工判断的内容再记录对应新决定。

## 证据与数据纪律

- 原始数据允许读取但禁止覆盖；清洗、转换、特征构造均写入派生目录并保留来源与规则。模型代码按数据契约读取已审计的输入，通常为清洗后的派生数据。
- 不得虚构附件、字段、单位、参数、实验、指标、论文、数据来源或人工理由。
- 方法选择前必须完成与当前任务有关的数据就绪检查；无数据题允许用明确的 `no_data_reason` 通过。代码生成前必须通过 G2.5 人工方法确认。
- 只有在任务需要比较、提出性能提升声明，或存在有意义的参考方案时才要求参考方法。参考角色可以是经验基线、启发式、历史方案、小规模精确解或解析校验；确无合适参考时记录 `none_with_reason`。发生比较时，主方法和参考方法必须使用相同的数据、划分、单位和指标定义。
- 不得仅为提高单次指标而无依据地更换算法、删除变量或调大模型。变量删除和机理参数约简必须有可复现证据，并通过消融或稳定性检查。
- 发现效果不佳时，先归因于数据、特征、方法、参数、实现或指标定义，再决定修复、回退或缩小声明。
- 正式完整赛题在方法讨论前做有边界的学术证据扫描。按实际证据需要选择来源扫描、摘要核对或全文分析，不因进入 submission 就强制下载并分析全文。优先级为：同方向高质量论文、官方资料与标准 → 数学结构相同的邻近领域学术资料 → 资料仍不足时才参考相似赛题解法。相似赛题解法只能标记为 `inspiration_only`，不能作为参数或结论的唯一依据。
- 学术证据充分性按问题机理、方法适用条件、验证/参考、数据/参数依据判断，不按固定篇数凑数；检索停止时记录未覆盖项和原因。
- 正式结果交接顺序为：计算与代码审查 → 初步结果诊断 → 按风险执行鲁棒性分析 → 汇总可追溯结果证据 → 按 profile 进行人工结果判断 → 生成正式求解过程与结果展示。若初步诊断发现实现错误、不可行或明显退化，先修复对应层，不强制完成整套鲁棒性与展示。正式展示从同一结果证据派生，不另维护竞争性的数值来源。

## Git 与算法实验

- 本项目使用 Git 保存 Harness 和建模实验。`main` 只承载已接受的稳定方案；一个已批准的方法族或模型结构使用一个 `exp/<contest>/<Qx>/<algorithm>` 分支。相同结构内的实现修复、参数、种子和重复运行使用新的 run ID 与收据，不为每次运行新建分支。
- 讨论本身不创建分支。用户明确同意尝试算法后，先确认工作区无未归属改动并保存稳定 checkpoint，再由 `git-experiment-manager` 创建实验分支。
- 每个实验摘要必须记录 commit、父 commit、数据/划分/特征/指标定义哈希、环境和随机种子。只有这些比较契约一致时才能宣称两个算法可直接比较。
- 正式执行顺序为明确代码/配置 checkpoint → 通过 Git 实验运行器执行并生成日志和收据 → 审查 → 保存证据提交。修复代码后使用新 run ID 和执行记录，不能把旧结果绑定到新代码；只有数据定义、特征口径、方法结构或指标含义发生变化时才要求新实验分支。
- 首次实验可从稳定提交分叉；方法、数据定义、特征口径或指标含义变化时可从保存好的当前 checkpoint 建立后继分支，记录父提交并保留旧实验。继承旧实验不代表接受它。仅实现或参数调整留在当前方法分支并创建新 run；纯拒绝返回稳定分支，启用结构不同的备选才建立后继分支。
- 接受方案后合并到稳定分支；拒绝方案保留分支与结果记录并返回稳定分支。已合并方案需要回退时优先 `git revert`，禁止为算法回退使用 `git reset --hard`。
- 不自动暂存或提交与当前实验无关的用户改动，不提交密钥、原始大数据、缓存、环境目录或大型模型；这些大文件用来源路径和哈希登记。

## 工作区约束

- 不为尚未开始的赛题预建完整目录；实际开始时按需创建。
- Git 已由用户明确要求并纳入 Harness。新赛题只在实际初始化时创建必要分支和状态，不为未开始的赛题制造提交。
- 不使用或搬动项目根目录下空的 `skills` 目录；项目级 Skills 固定存放在 `.agents/skills/`。

## 公共汇报与变更范围

- 讨论模式自然交流；正式调度汇报 profile、Qx/阶段门、证据或阻塞项、Git 实验上下文、一个主要下一动作及必要的人工判断理由。该约定针对调度消息，不限制专业分析或求解过程展示。
- 只重跑受影响的步骤与下游；原有决定与产物未受影响时复用。模型、数据、特征或指标变化按证据依赖传播。

## 写作表达：减少术语与套话

- 优先使用清楚、平实的语言。只有在有助于解释观点、工作内容或结果时才引入技术细节，并根据用户的问题和背景知识调整解释深度，使复杂概念的表达清晰、连贯。
- 避免空泛、程式化的词语和句式，例如把“深入探讨”“促进”“借助”“值得注意的是”“重要的是”“真正地”当作套话使用，或使用“核心结论：”“问题？答案。”以及“这不是关于 X，而是关于 Y”等固定句式。避免生造复合描述或形容词，也不要用“总之……”“最简单的理解方式是……”等句式机械地重复收尾。
- 直接说明要采取的行动。不要无端补充自己不会做什么、哪些内容保持不变，或将如何拆分、分类结果。避免用“X，而不是 Y”或“X——不是 Y”引入用户没有提出的对照选项。
- 不要生造含义不清的复合标签，例如“精确提交头检查”“编辑行布局”；避免含糊的限定语和模板化过渡句。使用常见词语、具体例子、准确的动词和介词，把关系直接说清楚。

上述要求针对无信息量的表达；数学术语、变量定义、必要的条件限定和有证据的方案比较仍应准确保留，正式结果展示应给出实质性的结论。
