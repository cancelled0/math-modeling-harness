# 数学建模项目规则

本文件只约束 `F:\compititions\数模` 及其子目录中的任务。

## 公共规则的唯一来源

- 本文件统一定义项目的模式、默认配置、人工判断、证据、Git、交付和汇报规则；项目级 Skills 只补充专业操作与产物契约。
- 用户当前明确要求和已有授权优先于 Skill 的默认建议，但不能越过系统、开发者指令或工具权限。赛题配置可覆盖默认值，AI 建议不能记作人工决定。
- Harness 维护任务不初始化赛题状态，也不经过方法选择或数值冻结门。

## 默认入口

- 完整赛题、目标模糊或跨阶段任务进入 `math-modeling-manager`；明确的单项任务可直接调用专业 Skill，完成后由 `workflow-orchestrator` 更新状态。
- 用户要求讨论或评价思路时使用 `modeling-thought-partner`。自由讨论可在任意阶段主动抽象、推导、比较或推翻方案，不更新状态、生成正式产物或作出方法决定；用户要求落地时返回总入口。
- `math-modeling-manager` 判断阶段和路由，`workflow-orchestrator` 管理状态与阶段门，专业 Skills 完成实际工作。

## 默认配置

- 默认使用 CUMCM、简体中文和 `submission`；局部实验、学习或临时分析使用 `lean`。主要使用 Python，用户明确指定其他语言时遵从。
- 中文 CUMCM 默认以 `paper/main.tex` 为唯一权威源，由 `latex-paper-zh` 构建并核对 PDF，再从同一源派生 DOCX；用户可明确改用 Word 或 Markdown 主格式。

## 自动推进与人工判断

在权限范围内自动推进文件盘点、数据处理、计算、代码执行、检查和状态更新。只在下列判断会实质改变模型或论文声明时暂停：

1. 题意或成功标准存在重大歧义；
2. 最终方法与可用基线的选择；
3. 结果接受、调整或启用备选方案；
4. 数值冻结以及论文声明范围。

- 人工决定由 `modeler-decision-logger` 写入每问的 JSONL 账本。已有明确决定直接记录，不重复询问；用户未说明理由时记为 `null`。
- AI 可以基于证据提出假设、推导、解释和贡献分析，但不得冒充用户决定，也不为这些内容逐项增加确认。
- 数据或权限缺失、工具不可用属于执行阻塞。应如实说明，并继续其他独立且已授权的工作。
- 状态机按当前输入、输出、依赖和决定的证据绑定判断步骤是否有效；证据发生实质变化时重验受影响部分。

## 证据与数据纪律

- 原始数据可以读取但不能覆盖；清洗、转换和特征构造写入派生目录并保留来源与规则。不得虚构数据、参数、实验、指标、文献或人工理由。
- 方法选择前完成必要的数据检查，代码生成前通过方法确认，论文写作前完成结果判断和冻结。
- 主方法和基线必须使用相同的数据、划分、单位和指标定义。不得为提高单次指标而无依据地更换算法、扩大模型或删除变量；变量约简须有消融或稳定性证据。
- 效果不佳时先区分数据、特征、方法、参数、实现或指标问题，再决定修复、回退或缩小声明。
- 正式赛题在方法讨论前进行有边界的证据扫描：优先同方向论文、官方资料与标准，其次数学结构相同的邻近领域资料；资料仍不足时才参考相似赛题，并标记为 `inspiration_only`。充分性按机理、适用条件、验证/基线和数据/参数依据判断，停止时记录缺口。
- 计算、代码审查、结果报告和鲁棒性完成后，先展示逐问求解过程与结果，再进入结果判断、冻结和论文写作。

## Git 与算法实验

- `main` 保存已接受的稳定方案。用户同意尝试算法后，保存当前可复现 checkpoint，再在 `exp/<contest>/<Qx>/<algorithm>` 分支实施。
- 实验摘要记录代码与父提交、数据/划分/特征/指标哈希、环境和随机种子。执行顺序为代码/配置 checkpoint → Git 实验运行器执行并生成日志、收据 → 审查 → 保存证据；修复代码后重新执行。
- 调整或备选方案从保存好的 checkpoint 建立后继实验并保留旧结果。接受后合并，拒绝后保留实验分支并返回稳定分支；已合并方案用 `git revert` 回退。
- 不暂存或提交无关改动、密钥、原始大数据、缓存、环境目录或大型模型；大文件登记路径和哈希。

## 工作区约束

- 实际赛题开始时才初始化必要目录、状态和分支。项目级 Skills 固定放在 `.agents/skills/`，不使用或搬动根目录下的空 `skills` 目录。
- 只重跑受影响的步骤及其下游，复用未受影响的决定和产物。冻结内容变化时记录解冻、重跑、重新冻结并审计一致性。
- DOCX 用于审阅或 Word 交付，不能反向覆盖 `.tex`。Word 或 Overleaf 中的人工修改须同步回本地 TeX、用 Git 提交，再重新生成 PDF/DOCX。

## 公共汇报与变更范围

- 讨论模式自然交流。正式调度说明当前问题与阶段、证据或阻塞、Git 实验上下文、一个主要下一动作和必要的暂停理由；专业分析、结果展示和论文正文不受该汇报格式限制。

## 写作表达

- 默认使用清晰、简洁的段落，每个段落阐述一个主要观点。只有在信息确实彼此并列、具有先后顺序，或用列表更便于比较时，才使用列表；除非无法用正文清楚表达层级关系，否则避免嵌套列表。使用通俗、简明的语言：熟悉的词语、具体的例子和准确的动词。优先使用主动语态和直接陈述。
- 务必尽早清楚地说明主要观点，然后用读者所需的解释和细节加以展开。让每句话承接前文。充分阐述重要观点，并提供足够的说明，使内容有用。
- 优先使用通俗语言，而不是行话；提及技术细节的程度，应以帮助用户理解某个观点或你的工作所需为限。清晰、连贯地表达复杂概念，并根据用户提示和上下文所体现的背景知识水平调整写作。
- 避免使用空泛、低质的词语或短语，例如在结论中使用“核心结论：”，或使用“深入探讨”“促进”“利用”“值得注意的是”“重要的是”“问题？答案。”“这不是关于 X，而是关于 Y。”“真正地”，以及用连字符连接的复合描述和形容词。不要使用“简而言之：……”或“最简单的心智模型是：……”之类的总结性结尾陈述。
- 直接说明要采取的行动。避免补充你不会做什么、哪些内容会保持不变，以及你将如何拆分或分类结果。不要使用“X，而不是 Y”或“X——不是 Y”等会引入用户没有要求的替代方案的对比框架。避免生造“精确标题检查”（exact-head checks）和“编辑行布局”（editorial-row layouts）之类的复合标签，也要避免含糊的限定语和程式化的过渡句；使用普通动词和介词直接说明实际关系。
- 数学术语、变量定义、必要条件和有证据的比较应准确保留，论文须给出实质结论。
