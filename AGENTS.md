# 数学建模项目规则

本文件只约束 `F:\compititions\数模` 及其子目录中的任务。

## 公共规则的唯一来源

- 本文件集中定义项目的模式、默认配置、授权与人工判断、证据纪律、Git、交付和公共汇报规则。项目级 Skills 与 references 引用本文件，不另行维护不同版本的公共政策。
- 用户当前明确要求与已有授权优先于 Skill 的默认建议；不得据此越过系统、开发者指令或工具权限。赛题配置可覆盖比赛、语言和交付默认值，不能把 AI 建议记成人工决定。
- Skill 保留本领域的输入输出、算法说明、操作步骤和检查项；其中的前置条件用于核验证据，不代表增加一次人工确认。程序中的字段、阶段门和校验属于可执行契约，应与本文件一致。
- 明确的 Harness 维护任务不初始化赛题状态，也不套用赛题方法选择或数值冻结门。

## 默认入口

- 完整赛题、目标模糊的请求、跨阶段任务，以及需要继续既有建模工作的请求，优先使用 `math-modeling-manager`。
- 用户明确说“先讨论思路”“评价方案”“暂不写代码”时，使用 `modeling-thought-partner`。讨论模式不更新阶段门、不生成正式产物，也不把建议当作方法决定；用户明确要求正式求解、采用方法或开始实现时再回到总入口。
- 自由讨论可在任意建模阶段进行：允许主动抽象、推导、提出新模型、比较或推翻方案，不受固定 Skill 顺序、既定算法清单或阶段门限制。阶段门约束正式执行和成果接受；讨论中的假设与猜想应明确标识，已有证据与事实仍需如实表达。
- 用户明确要求单项工作时，可以直接使用对应专业 Skill；完成后把产物交回 `workflow-orchestrator` 更新状态。
- `math-modeling-manager` 负责判断阶段和路由，`workflow-orchestrator` 负责 manifest、阶段门和下一动作，专业 Skill 负责实际工作。不要让总入口重复执行专业分析。

## 默认配置

- 默认比赛为 CUMCM，工作语言和论文语言为简体中文。
- 完整赛题默认 `rigor_profile=submission`；局部实验、学习、临时分析默认 `lean`。
- 自动选择实现语言时优先 Python；用户指定 MATLAB、北太天元，或现有主工程为 `.m` 时使用 MATLAB 路径。
- 中文 CUMCM 默认使用 `delivery_mode=latex_primary_docx_mirror`：`paper/main.tex` 是唯一权威论文源，先用 `latex-paper-zh` 生成并核对 PDF，再由同一 Skill 通过 Pandoc 派生 `paper/exports/main.docx`。编译前必须检查 XeLaTeX/latexmk、Pandoc、字体与模板能力；工具链不可用时明确报告，不得伪造成功。显式选择 Word/Markdown 主格式时仍可走独立备选路径。

## 自动推进与人工判断

在权限范围内自动推进文件盘点、数据剖析、可复现计算、代码执行、检查和状态更新。仅在下列判断会实质改变模型或论文声明时暂停确认：

1. 题意或成功标准存在重大歧义；
2. 最终方法与可用基线的选择；
3. 结果接受、调整或启用备选方案；
4. 数值冻结以及论文声明范围。

人工决定必须由 `modeler-decision-logger` 记入每问的 JSONL 决策账本。不得把 AI 建议当成人工决定。

- 已明确选定的方法直接记录，不重复询问同一选择；用户未给出理由时记录为 null，不要求补写理由。探索性赞同仍不等于正式决定。
- 模型假设的必要性、物理解释、推导和贡献分析可由 AI 基于证据提出；最终采用受上述方法、结果及声明范围判断约束，不逐条新增确认。
- 学术证据扫描是正式完整求解的准备步骤，不阻止用户在任意阶段主动发起自由讨论。
- 缺失实际数据、工具不可用或缺少访问权限属于执行阻塞，不是新增的建模判断类别；如实说明，不伪造通过。因局部问题受阻时可继续独立且已授权的工作。
- 已完成步骤是否有效，以当前输入、输出、依赖和决定的哈希绑定为准；不能仅凭文件存在或时间戳较新认定有效。证据实质改变后重新验证，涉及既有人工判断的内容再记录对应新决定。

## 证据与数据纪律

- 原始数据允许读取但禁止覆盖；清洗、转换、特征构造均写入派生目录并保留来源与规则。模型代码按数据契约读取已审计的输入，通常为清洗后的派生数据。
- 不得虚构附件、字段、单位、参数、实验、指标、论文、数据来源或人工理由。
- 方法选择前必须完成必要的数据就绪检查；代码生成前必须通过 G2.5 人工方法确认；论文写作前必须完成结果判断与冻结。
- 主方法和可用基线必须在同一数据、划分、单位和指标定义下比较。
- 不得仅为提高单次指标而无依据地更换算法、删除变量或调大模型。变量删除和机理参数约简必须有可复现证据，并通过消融或稳定性检查。
- 发现效果不佳时，先归因于数据、特征、方法、参数、实现或指标定义，再决定修复、回退或缩小声明。
- 正式完整赛题在方法讨论前做有边界的学术证据扫描。优先级为：同方向高质量论文、官方资料与标准 → 数学结构相同的邻近领域学术资料 → 资料仍不足时才参考相似赛题解法。相似赛题解法只能标记为 `inspiration_only`，不能作为参数或结论的唯一依据。
- 学术证据充分性按问题机理、方法适用条件、验证/基线、数据/参数依据判断，不按固定篇数凑数；检索停止时记录未覆盖项和原因。
- 正式结果交接顺序为：计算与代码审查 → 结果报告 → 鲁棒性分析 → 求解过程与结果展示 → 人工结果判断 → 方法解释与数值/声明冻结 → 论文写作。结果展示包含假设、准备、推导、逐问模型与理由、算法、结果来源、结论和局限；不适用项可说明原因。

## Git 与算法实验

- 本项目使用 Git 保存 Harness 和建模实验。`main` 只承载已接受的稳定方案；算法或模型结构变化在 `exp/<contest>/<Qx>/<algorithm>` 分支实施。
- 讨论本身不创建分支。用户明确同意尝试算法后，先确认工作区无未归属改动并保存稳定 checkpoint，再由 `git-experiment-manager` 创建实验分支。
- 每个实验摘要必须记录 commit、父 commit、数据/划分/特征/指标定义哈希、环境和随机种子。只有这些比较契约一致时才能宣称两个算法可直接比较。
- 正式执行顺序为明确代码/配置 checkpoint → 通过 Git 实验运行器执行并生成日志和收据 → 审查 → 保存证据提交。修复代码后使用新执行记录，不能把旧结果绑定到新代码。
- 首次实验可从稳定提交分叉；已授权的调整可从保存好的当前 checkpoint 建立后继分支，记录父提交并保留旧实验。继承旧实验不代表接受它。纯拒绝返回稳定分支，调整/备选则按当前决定启动后继实验。
- 接受方案后合并到稳定分支；拒绝方案保留分支与结果记录并返回稳定分支。已合并方案需要回退时优先 `git revert`，禁止为算法回退使用 `git reset --hard`。
- 不自动暂存或提交与当前实验无关的用户改动，不提交密钥、原始大数据、缓存、环境目录或大型模型；这些大文件用来源路径和哈希登记。

## 工作区约束

- 不为尚未开始的赛题预建完整目录；实际开始时按需创建。
- Git 已由用户明确要求并纳入 Harness。新赛题只在实际初始化时创建必要分支和状态，不为未开始的赛题制造提交。
- 不使用或搬动项目根目录下空的 `skills` 目录；项目级 Skills 固定存放在 `.agents/skills/`。
- 修改冻结数字或论文声明时，执行解冻记录、受影响流程重跑、重新冻结与范围一致性审计。
- DOCX 只用于审阅或要求 Word 的交付，不是可反向覆盖 `.tex` 的权威源。Word 中的人工修改必须先同步回 LaTeX，再重新生成 PDF/DOCX；Overleaf 修改也必须同步回本地 `.tex` 并用 Git 提交后再导出，禁止维护两份相互漂移的正文。

## 公共汇报与变更范围

- 讨论模式自然交流；正式调度汇报 profile、Qx/阶段门、证据或阻塞项、Git 实验上下文、一个主要下一动作及必要的人工判断理由。该约定针对调度消息，不限制专业分析、求解过程展示或论文正文。
- 只重跑受影响的步骤与下游；原有决定与产物未受影响时复用。模型、数据、特征或指标变化按证据依赖传播；冻结内容变化执行解冻和重新冻结。
