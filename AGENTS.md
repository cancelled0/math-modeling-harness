# 数学建模项目规则

本文件只约束 `F:\compititions\数模` 及其子目录中的任务。

## 默认入口

- 完整赛题、目标模糊的请求、跨阶段任务，以及需要继续既有建模工作的请求，优先使用 `math-modeling-manager`。
- 用户明确说“先讨论思路”“评价方案”“暂不写代码”时，使用 `modeling-thought-partner`。讨论模式不更新阶段门、不生成正式产物，也不把建议当作方法决定；用户明确要求正式求解、采用方法或开始实现时再回到总入口。
- 用户明确要求单项工作时，可以直接使用对应专业 Skill；完成后把产物交回 `workflow-orchestrator` 更新状态。
- `math-modeling-manager` 负责判断阶段和路由，`workflow-orchestrator` 负责 manifest、阶段门和下一动作，专业 Skill 负责实际工作。不要让总入口重复执行专业分析。

## 默认配置

- 默认比赛为 CUMCM，工作语言和论文语言为简体中文。
- 完整赛题默认 `rigor_profile=submission`；局部实验、学习、临时分析默认 `lean`。
- 自动选择实现语言时优先 Python；用户指定 MATLAB、北太天元，或现有主工程为 `.m` 时使用 MATLAB 路径。
- 中文 CUMCM 默认保留中文 LaTeX→PDF 路径，使用 `latex-paper-zh`；编译前必须检查 XeLaTeX/latexmk、字体与模板能力。工具链不可用时明确报告，不得伪造编译成功；Word/Markdown 是可配置的备选交付路径。

## 自动推进与人工判断

在权限范围内自动推进文件盘点、数据剖析、可复现计算、代码执行、检查和状态更新。仅在下列判断会实质改变模型或论文声明时暂停确认：

1. 题意或成功标准存在重大歧义；
2. 最终方法与可用基线的选择；
3. 结果接受、调整或启用备选方案；
4. 数值冻结以及论文声明范围。

人工决定必须由 `modeler-decision-logger` 记入每问的 JSONL 决策账本。不得把 AI 建议当成人工决定。

## 证据与数据纪律

- 原始数据只读；清洗、转换、特征构造均写入派生目录并保留来源与规则。
- 不得虚构附件、字段、单位、参数、实验、指标、论文、数据来源或人工理由。
- 方法选择前必须完成必要的数据就绪检查；代码生成前必须通过 G2.5 人工方法确认；论文写作前必须完成结果判断与冻结。
- 主方法和可用基线必须在同一数据、划分、单位和指标定义下比较。
- 不得仅为提高单次指标而无依据地更换算法、删除变量或调大模型。变量删除和机理参数约简必须有可复现证据，并通过消融或稳定性检查。
- 发现效果不佳时，先归因于数据、特征、方法、参数、实现或指标定义，再决定修复、回退或缩小声明。
- 正式完整赛题在方法讨论前做有边界的学术证据扫描。优先级为：同方向高质量论文、官方资料与标准 → 数学结构相同的邻近领域学术资料 → 资料仍不足时才参考相似赛题解法。相似赛题解法只能标记为 `inspiration_only`，不能作为参数或结论的唯一依据。

## Git 与算法实验

- 本项目使用 Git 保存 Harness 和建模实验。`main` 只承载已接受的稳定方案；算法或模型结构变化在 `exp/<contest>/<Qx>/<algorithm>` 分支实施。
- 讨论本身不创建分支。用户明确同意尝试算法后，先确认工作区无未归属改动并保存稳定 checkpoint，再由 `git-experiment-manager` 创建实验分支。
- 每个实验摘要必须记录 commit、父 commit、数据/划分/特征/指标定义哈希、环境和随机种子。只有这些比较契约一致时才能宣称两个算法可直接比较。
- 接受方案后合并到稳定分支；拒绝方案保留分支与结果记录并返回稳定分支。已合并方案需要回退时优先 `git revert`，禁止为算法回退使用 `git reset --hard`。
- 不自动暂存或提交与当前实验无关的用户改动，不提交密钥、原始大数据、缓存、环境目录或大型模型；这些大文件用来源路径和哈希登记。

## 工作区约束

- 不为尚未开始的赛题预建完整目录；实际开始时按需创建。
- Git 已由用户明确要求并纳入 Harness。新赛题只在实际初始化时创建必要分支和状态，不为未开始的赛题制造提交。
- 不使用或搬动项目根目录下空的 `skills` 目录；项目级 Skills 固定存放在 `.agents/skills/`。
- 修改冻结数字或论文声明时，执行解冻记录、受影响流程重跑、重新冻结与范围一致性审计。
